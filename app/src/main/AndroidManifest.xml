<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erdierdal.zaferjongeren" >
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--Service declaration.-->
        <service
            android:enabled="true" android:name=".userfunctions.ServiceHandler" >
        </service>

        <activity
            android:name=".screens.SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".screens.MainMenu"
            android:label="@string/title_activity_main_menu">
        </activity>
        <activity
            android:name=".screens.Activiteiten"
            android:label="@string/title_activity_activiteiten">
        </activity>
        <activity
            android:name=".userfunctions.NotificationHandler"
            android:label="@string/title_activity_notification_handler">
        </activity>
    </application>

</manifest>
